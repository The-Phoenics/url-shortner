import express, { Router } from "express"

const userRouter: Router = express.Router();

userRouter.get("/urls", (req, res) => {
    
})

userRouter.post("/urls", (req, res) => {

})

export default userRouter;