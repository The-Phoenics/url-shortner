{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.mjs"
  },
  "dependencies": {
    "@types/rate-limit-redis": "^3.0.0",
    "@url-shortner/auth": "workspace:*",
    "@url-shortner/cache": "workspace:*",
    "@url-shortner/db": "workspace:*",
    "@url-shortner/env": "workspace:*",
    "@url-shortner/queue": "workspace:*",
    "better-auth": "catalog:",
    "cors": "^2.8.5",
    "dotenv": "catalog:",
    "express": "^5.1.0",
    "express-rate-limit": "^8.5.2",
    "rate-limit-redis": "^5.0.0",
    "sqids": "^0.3.0",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.1",
    "@types/express-rate-limit": "^6.0.2",
    "@url-shortner/config": "workspace:*",
    "@url-shortner/shared": "workspace:*",
    "tsdown": "^0.21.9",
    "typescript": "catalog:"
  }
}
